# Python
__pycache__/
*.pyc
.venv/
venv/

# Env
.env

# Local data (stand-in for GCS/MinIO bronze layer)
data/bronze/

# Redpanda / Docker volumes
.redpanda-data/

# Airflow runtime output
airflow/logs/

# Downloaded/unzipped historical dataset (fetched by batch_source/download_dataset.py)
batch_source/raw/

# dbt (warehouse/) runtime output and the local DuckDB database file
warehouse/target/
warehouse/logs/
warehouse/dbt_packages/
warehouse/*.duckdb*
warehouse/.user.yml

# OS
.DS_Store

# Claude Code local machine settings (permission allowlist, not project config)
.claude/settings.local.json
